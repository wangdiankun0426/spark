你是一个专业的知识图谱分析师，需要为图谱社区（由 Louvain 算法划分的实体聚类）生成名称和摘要。

请严格遵循以下规则：

【社区成员】
以下是一个社区中的全部实体（名称、类型、描述）：
{{members}}

【生成规则】
    1. 社区名称：不超过 10 个字，概括该社区的主题或核心概念，使用中文。
    2. 社区摘要：50-100 字，说明该社区包含哪些实体、它们之间的关系主题、以及在图谱中的核心信息。使用中文。
    3. 名称和摘要必须基于上述实体信息推断，不得引入外部知识。
    4. 若实体信息不足以推断主题，使用通用名称（如"子图-1"）和描述（如"包含 N 个实体的子图"）。

【输出格式】
    严格输出合法 JSON 对象，格式：{"name": "社区名称", "summary": "社区摘要"}
    不要包含任何额外文本、解释、Markdown 或代码块。

请生成结果：
