<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.spark.dao.sys.UserDao" >
    <sql id="Base_Column_List" >
     su.id , su.login_name , su.name , su.phone , su.email , su.sex , su.avatar , su.status , su.account_type , su.current_tenant_id , su.delete_flag ,su.created_by , su.created_dt , su.updated_by , su.updated_dt
    </sql>

    <sql id="condition">
        <if test="id != null">
            and su.id = #{id}
        </if>
        <if test="ids != null and ids.size() > 0">
            and su.id in (
            <foreach collection="ids" item="id" separator=",">
                #{id}
            </foreach>
            )
        </if>
        <if test="loginName != null">
            and su.login_name = #{loginName}
        </if>
        <if test="name != null">
            and su.name like concat('%', #{name}, '%')
        </if>
        <if test="phone != null">
            and su.phone = #{phone}
        </if>
        <if test="email != null">
            and su.email = #{email}
        </if>
        <if test="sex != null">
            and su.sex = #{sex}
        </if>
        <if test="status != null">
            and su.status = #{status}
        </if>
        <if test="deleteFlag != null and id == null and ids == null">
            and su.delete_flag = #{deleteFlag}
        </if>
        <if test="deptId != null">
            and stu.dept_id in (
            select id from sys_department where code like (select CONCAT(code, '%') from sys_department where id = #{deptId})
            )
        </if>
        <if test="deptIds != null and deptIds.size() > 0">
            and stu.dept_id in (
            <foreach collection="deptIds" item="deptId" separator=",">
                #{deptId}
            </foreach>
            )
        </if>
        <if test="tenantId != null">
            and stu.tenant_id = #{tenantId}
        </if>
    </sql>

    <insert id="insert">
        insert into sys_user(id , login_name , password , name ,  phone ,  email , sex , avatar , status , account_type, current_tenant_id,  delete_flag , created_by ,  created_dt ,  updated_by ,  updated_dt )
        values (#{id}, #{loginName}, #{password}, #{name}, #{phone}, #{email}, #{sex}, #{avatar}, #{status}, #{accountType}, #{currentTenantId}, 1, #{createdBy}, #{createdDt}, #{updatedBy}, #{updatedDt})
    </insert>

    <select id="queryUserList" resultType="com.spark.common.bean.sys.result.UserResult">
        select <include refid="Base_Column_List"/>
        <if test="deptId != null or deptIds != null or tenantId != null">
            , stu.tenant_id, stu.dept_id, stu.role_type
        </if>
        from  sys_user su
        <if test="deptId != null or deptIds != null or tenantId != null">
            left join sys_tenant_user stu on (su.id = stu.user_id and stu.delete_flag = 1)
        </if>
        where 1=1
        <include refid="condition"/>
        ${dataScopeSQL}
        order by su.id
        <if test="page == true">
            limit #{startRow} , #{pageSize}
        </if>
    </select>

    <select id="queryUserCount" resultType="java.lang.Integer">
        select count(1)
        from  sys_user su
        <if test="deptId != null or deptIds != null or tenantId != null">
            left join sys_tenant_user stu on (su.id = stu.user_id and stu.delete_flag = 1)
        </if>
        where 1=1
        <include refid="condition"/>
        ${dataScopeSQL}
    </select>

    <select id="queryUser" resultType="com.spark.common.bean.sys.result.UserResult">
        select <include refid="Base_Column_List"/>,  password
        <if test="deptId != null or deptIds != null or tenantId != null">
            , stu.tenant_id, stu.dept_id, stu.role_type
        </if>
        from  sys_user su
        <if test="deptId != null or deptIds != null or tenantId != null">
            left join sys_tenant_user stu on (su.id = stu.user_id and stu.delete_flag = 1)
        </if>
        where 1=1
        <include refid="condition"/>
        limit 1
    </select>

    <update id="updateById" parameterType="com.spark.common.bean.sys.entity.User">
        update  sys_user
        set updated_by = #{updatedBy}, updated_dt = #{updatedDt}
        <if test="name != null">
            ,name = #{name}
        </if>
        <if test="password != null">
            ,password = #{password}
        </if>
        <if test="loginName != null">
            ,login_name = #{loginName}
        </if>
        <if test="phone != null">
            ,phone = #{phone}
        </if>
        <if test="email != null">
            ,email = #{email}
        </if>
        <if test="sex != null">
            ,sex = #{sex}
        </if>
        <if test="avatar != null">
            ,avatar = #{avatar}
        </if>
        <if test="status != null">
            ,status = #{status}
        </if>
        <if test="currentTenantId != null">
            ,current_tenant_id = #{currentTenantId}
        </if>
        where id = #{id}
    </update>

    <delete id="deleteById" parameterType="com.spark.common.bean.sys.entity.User">
        update  sys_user
        set updated_by = #{updatedBy}, updated_dt = #{updatedDt}, delete_flag = -1
        where id = #{id}
    </delete>

</mapper>
