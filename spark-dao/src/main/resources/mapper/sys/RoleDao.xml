<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.spark.dao.sys.RoleDao" >
    <sql id="Base_Column_List" >
     id , name ,  data_scope ,  status ,  delete_flag , created_by ,  created_dt ,  updated_by ,  updated_dt
    </sql>

    <sql id="condition">
        <if test="id != null">
            and id = #{id}
        </if>
        <if test="status != null">
            and status = #{status}
        </if>
        <if test="deleteFlag != null and id == null and ids == null">
            and delete_flag = #{deleteFlag}
        </if>
    </sql>

    <select id="querySameNameRole" resultType="com.spark.common.bean.sys.result.RoleResult">
        select <include refid="Base_Column_List"/>
        from  sys_role
        where name = #{name}
        limit 1
    </select>

    <insert id="insert">
        insert into  sys_role  ( id , name ,  data_scope ,  status ,  delete_flag , created_by ,  created_dt ,  updated_by ,  updated_dt )
        values (#{id}, #{name}, #{dataScope}, #{status}, 1, #{createdBy}, #{createdDt}, #{updatedBy}, #{updatedDt})
    </insert>

    <select id="queryRoleList" resultType="com.spark.common.bean.sys.result.RoleResult">
        select <include refid="Base_Column_List"/>
        from  sys_role
        where 1=1
        <include refid="condition"/>
        <if test="page == true">
            limit #{startRow}, #{pageSize}
        </if>
    </select>

    <select id="queryRole" resultType="com.spark.common.bean.sys.result.RoleResult">
        select <include refid="Base_Column_List"/>
        from  sys_role
        where 1=1
        <include refid="condition"/>
        limit 1
    </select>

    <select id="queryRoleCount" resultType="java.lang.Integer">
        select count(1)
        from  sys_role
        where 1=1
        <include refid="condition"/>
    </select>

    <update id="updateById">
        update  sys_role
        set  updated_by = #{updatedBy}, updated_dt = #{updatedDt}
        <if test="name != null">
            ,name = #{name}
        </if>
        <if test="dataScope != null">
            ,data_scope = #{dataScope}
        </if>
        <if test="status != null">
            ,status = #{status}
        </if>
        where id = #{id}
    </update>

    <update id="deleteById">
        update  sys_role
        set delete_flag = -1, updated_by = #{updatedBy}, updated_dt = #{updatedDt}
        where id = #{id}
    </update>

    <select id="queryUserMaxDataScope" resultType="java.lang.Integer">
        select IFNULL(max(data_scope),1)
        from  sys_role
        where delete_flag = 1 and id in (select role_id from sys_role_user where user_id = #{userId})
    </select>
</mapper>
