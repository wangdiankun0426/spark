<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.spark.dao.flow.FlowTemplateSequenceDao" >
    <sql id="allColumnList" >
        id , template_id , rev_id , sequence_id, source_ref , target_ref, `condition` , delete_flag , created_by , created_dt , updated_by , updated_dt
    </sql>

    <sql id="searchCondition">

    </sql>

    <insert id="batchInsert">
        insert into flow_template_sequence (template_id , rev_id , sequence_id, source_ref , target_ref, `condition` , delete_flag , created_by , created_dt , updated_by , updated_dt)
        values
        <foreach item="item" index="index" collection="list" separator=",">
            ( #{templateId} , #{revId} , #{item.sequenceId} , #{item.sourceRef} , #{item.targetRef} , #{item.condition} , 1, 101, now(), 101,now())
        </foreach>
    </insert>

</mapper>
