<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.spark.dao.workflow.WfTemplateEndpointDao" >
    <sql id="allColumnList" >
        id , template_id , rev_id , path , auth_type , api_key , enabled , delete_flag , created_by , created_dt , updated_by , updated_dt
    </sql>

    <sql id="searchCondition">
        <if test="id != null">
            and id = #{id}
        </if>
        <if test="ids != null and ids.size() > 0">
            and id in (
            <foreach collection="ids" item="id" separator=",">#{id}</foreach>
            )
        </if>
        <if test="templateId != null">
            and template_id = #{templateId}
        </if>
        <if test="path != null and path != ''">
            and path = #{path}
        </if>
        <if test="deleteFlag != null and id == null and ids == null">
            and delete_flag = #{deleteFlag}
        </if>
    </sql>

    <sql id="sortCondition">
        <choose>
            <when test="sorts != null and sorts.size() > 0">
                <foreach item="value" index="key" collection="sorts.entrySet()" separator=",">${value}</foreach>
            </when>
            <otherwise>id desc</otherwise>
        </choose>
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into wf_template_endpoint ( template_id , rev_id , path , auth_type , api_key , enabled , delete_flag , created_by , created_dt , updated_by , updated_dt)
        values (#{templateId} , #{revId} , #{path} , #{authType} , #{apiKey} , #{enabled} , 1, #{createdBy}, #{createdDt}, #{updatedBy}, #{updatedDt})
    </insert>

    <update id="deleteById">
        update wf_template_endpoint set delete_flag = -1 , updated_by = #{updatedBy} , updated_dt = #{updatedDt} where id = #{id}
    </update>

    <update id="updateById">
        update wf_template_endpoint set updated_by = #{updatedBy} , updated_dt = #{updatedDt}
        <if test="path != null and path != ''">
            , path = #{path}
        </if>
        <if test="authType != null">
            , auth_type = #{authType}
        </if>
        <if test="apiKey != null">
            , api_key = #{apiKey}
        </if>
        <if test="enabled != null">
            , enabled = #{enabled}
        </if>
        where id = #{id}
    </update>

    <select id="queryEndpointCount" resultType="java.lang.Integer">
        select count(1)
        from wf_template_endpoint
        where 1 = 1
        <include refid="searchCondition"/>
    </select>

    <select id="queryEndpointList" resultType="com.spark.bean.workflow.result.WfTemplateEndpointResult">
        select <include refid="allColumnList"/>
        from wf_template_endpoint
        where 1 = 1
        <include refid="searchCondition"/>
        order by <include refid="sortCondition"/>
        <if test="page == true">
            limit #{startRow}, #{pageSize}
        </if>
    </select>

    <select id="queryEndpoint" resultType="com.spark.bean.workflow.result.WfTemplateEndpointResult">
        select <include refid="allColumnList"/>
        from wf_template_endpoint
        where 1=1
        <include refid="searchCondition"/>
         limit 1
    </select>
</mapper>
