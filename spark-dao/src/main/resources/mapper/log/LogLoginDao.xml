<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.spark.dao.log.LogLoginDao" >
    <sql id="Base_Column_List" >
    id, ipaddress,login_type,login_platform, session_id, delete_flag, created_dt, created_by,updated_dt, updated_by
    </sql>

    <sql id="condition">
        <if test="ipaddress != null ">
            AND ipaddress = #{ipaddress}
        </if>
        <if test="createdStartTime != null ">
            AND created_dt > #{createdStartTime}
        </if>
        <if test="createdEndTime != null ">
            AND #{createdEndTime} > created_dt
        </if>
        <if test="createdBy != null">
            AND created_by = #{createdBy}
        </if>
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into log_login (ipaddress,login_type,login_platform,session_id, delete_flag, created_dt, created_by,updated_dt, updated_by)
        values (#{ipaddress}, #{loginType}, #{loginPlatform}, #{sessionId}, 1 , now(), #{createdBy}, now(), #{updatedBy})
    </insert>

    <select id="queryLogLoginList" resultType="com.spark.common.bean.log.result.LogLoginResult">
        select <include refid="Base_Column_List"/>
        from log_login
        where 1=1
        <include refid="condition"/>
        order by id desc
        <if test="page == true">
            limit #{startRow}, #{pageSize}
        </if>
    </select>

    <select id="queryLogLoginCount" resultType="java.lang.Integer">
        select count(1)
        from log_login
        where 1=1
        <include refid="condition"/>
    </select>
</mapper>
