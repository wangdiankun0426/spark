<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.spark.dao.llm.McpDao" >
    <sql id="allColumnList" >
        id , name , transport , provider_id , command , args , url , env , timeout , description , status , dept_id , delete_flag , created_by , created_dt , updated_by , updated_dt
    </sql>

    <sql id="searchCondition">
        <if test="id != null">
            and id = #{id}
        </if>
        <if test="ids != null and ids.size() > 0">
            and id in (
            <foreach collection="ids" item="id" separator=",">
                #{id}
            </foreach>
            )
        </if>
        <if test="deleteFlag != null and id == null and ids == null">
            and delete_flag = #{deleteFlag}
        </if>
        <if test="status != null">
            and status = #{status}
        </if>
        <if test="transport != null">
            and transport = #{transport}
        </if>
        <if test="providerId != null">
            and provider_id = #{providerId}
        </if>
        <if test="name != null">
            and name like concat('%', #{name}, '%')
        </if>
    </sql>

    <sql id="sortCondition">
        <choose>
            <when test="sorts != null and sorts.size() > 0">
                <foreach item="value" index="key" collection="sorts.entrySet()" separator=",">
                    ${value}
                </foreach>
            </when>
            <otherwise>
                id desc
            </otherwise>
        </choose>
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into llm_mcp (name , transport , provider_id , command , args , url , env , timeout , description , status , dept_id , delete_flag , created_by , created_dt , updated_by , updated_dt)
        values (#{name} , #{transport} , #{providerId} , #{command} , #{args} , #{url} , #{env} , #{timeout} , #{description} , #{status} , #{deptId} , 1, #{createdBy}, #{createdDt}, #{updatedBy}, #{updatedDt})
    </insert>

    <update id="deleteById">
        update llm_mcp set delete_flag = -1 , updated_by = #{updatedBy} , updated_dt = #{updatedDt}
        where id = #{id}
    </update>

    <update id="updateById">
        update llm_mcp  set updated_by = #{updatedBy} , updated_dt = #{updatedDt}
        <if test="name != null and name != ''">
            , name = #{name}
        </if>
        <if test="transport != null">
            , transport = #{transport}
        </if>
        <if test="providerId != null">
            , provider_id = #{providerId}
        </if>
        <if test="command != null">
            , command = #{command}
        </if>
        <if test="args != null">
            , args = #{args}
        </if>
        <if test="url != null">
            , url = #{url}
        </if>
        <if test="env != null">
            , env = #{env}
        </if>
        <if test="timeout != null">
            , timeout = #{timeout}
        </if>
        <if test="description != null and description != ''">
            , description = #{description}
        </if>
        <if test="status != null">
            , status = #{status}
        </if>
        where id = #{id}
    </update>

    <select id="queryMcpCount" resultType="java.lang.Integer">
        select count(1)
        from llm_mcp
        where 1 = 1
        <include refid="searchCondition"/>
        ${dataScopeSQL}
    </select>

    <select id="queryMcpList" resultType="com.spark.common.bean.llm.result.McpResult">
        select <include refid="allColumnList"/>
        from llm_mcp
        where 1 = 1
        <include refid="searchCondition"/>
        ${dataScopeSQL}
        order by
        <include refid="sortCondition"/>
        <if test="page == true">
            limit #{startRow}, #{pageSize}
        </if>
    </select>

    <select id="queryMcp" resultType="com.spark.common.bean.llm.result.McpResult">
        select <include refid="allColumnList"/>
        from llm_mcp
        where 1=1
        <include refid="searchCondition"/>
        limit 1
    </select>
</mapper>
