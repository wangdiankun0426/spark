<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.spark.dao.llm.ModelDao" >
    <sql id="allColumnList" >
        id , provider_id , type , name , description , remark , enable_thinking , temperature , status , dept_id , delete_flag , created_by , created_dt , updated_by , updated_dt
    </sql>

    <sql id="searchCondition">
        <if test="id != null">
            and id = #{id}
        </if>
        <if test="ids != null and ids.size() > 0">
            and id in (
            <foreach collection="ids" item="id" separator=",">
                #{id}
            </foreach>
            )
        </if>
        <if test="providerId != null">
            and provider_id = #{providerId}
        </if>
        <if test="type != null">
            and type = #{type}
        </if>
        <if test="name != null and name != ''">
            and name like concat('%', #{name}, '%')
        </if>
        <if test="enableThinking != null">
            and enable_thinking = #{enableThinking}
        </if>
        <if test="status != null">
            and status = #{status}
        </if>
        <if test="deleteFlag != null and id == null and ids == null">
            and delete_flag = #{deleteFlag}
        </if>
    </sql>

    <sql id="sortCondition">
        <choose>
            <when test="sorts != null and sorts.size() > 0">
                <foreach item="value" index="key" collection="sorts.entrySet()" separator=",">
                    ${value}
                </foreach>
            </when>
            <otherwise>
                id desc
            </otherwise>
        </choose>
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into llm_model ( id,provider_id , type , name , description , remark , enable_thinking , temperature , status , dept_id , delete_flag , created_by , created_dt , updated_by , updated_dt)
        values ( #{id}, #{providerId} , #{type} , #{name} , #{description} , #{remark} , IFNULL(#{enableThinking}, -1) , IFNULL(#{temperature}, 0.1) , IFNULL(#{status}, 1) , #{deptId} , 1, #{createdBy}, #{createdDt}, #{updatedBy}, #{updatedDt})
    </insert>

    <update id="deleteById">
        update llm_model set delete_flag = -1 , updated_by = #{updatedBy} , updated_dt = #{updatedDt}
        where id = #{id}
    </update>

    <update id="updateById">
        update llm_model  set updated_by = #{updatedBy} , updated_dt = #{updatedDt}
        <if test="providerId != null">
            , provider_id = #{providerId}
        </if>
        <if test="type != null">
            , type = #{type}
        </if>
        <if test="name != null and name != ''">
            , name = #{name}
        </if>
        <if test="description != null and description != ''">
            , description = #{description}
        </if>
        <if test="remark != null and remark != ''">
            , remark = #{remark}
        </if>
        <if test="enableThinking != null">
            , enable_thinking = #{enableThinking}
        </if>
        <if test="temperature != null">
            , temperature = #{temperature}
        </if>
        <if test="status != null">
            , status = #{status}
        </if>
        where id = #{id}
    </update>

    <select id="queryModelCount" resultType="java.lang.Integer">
        select count(1)
        from llm_model
        where 1 = 1
        <include refid="searchCondition"/>
        ${dataScopeSQL}
    </select>

    <select id="queryModelList" resultType="com.spark.bean.llm.result.ModelResult">
        select <include refid="allColumnList"/>
        from llm_model
        where 1 = 1
        <include refid="searchCondition"/>
        ${dataScopeSQL}
        order by
        <include refid="sortCondition"/>
        <if test="page == true">
            limit #{startRow}, #{pageSize}
        </if>
    </select>

    <select id="queryModel" resultType="com.spark.bean.llm.result.ModelResult">
        select <include refid="allColumnList"/>
        from llm_model
        where 1=1
        <include refid="searchCondition"/>
        limit 1
    </select>
</mapper>
