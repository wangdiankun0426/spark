<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.spark.dao.llm.SkillDao" >
    <sql id="allColumnList" >
        id , name , description , content , status , dept_id , delete_flag , created_by , created_dt , updated_by , updated_dt
    </sql>

    <sql id="listColumnList" >
        id , name , description , status , dept_id , delete_flag , created_by , created_dt , updated_by , updated_dt
    </sql>

    <sql id="searchCondition">
        <if test="id != null">
            and id = #{id}
        </if>
        <if test="ids != null and ids.size() > 0">
            and id in (
            <foreach collection="ids" item="id" separator=",">
                #{id}
            </foreach>
            )
        </if>
        <if test="deleteFlag != null and id == null and ids == null">
            and delete_flag = #{deleteFlag}
        </if>
        <if test="status != null">
            and status = #{status}
        </if>
        <if test="name != null">
            and name like concat('%', #{name}, '%')
        </if>
    </sql>

    <sql id="sortCondition">
        <choose>
            <when test="sorts != null and sorts.size() > 0">
                <foreach item="value" index="key" collection="sorts.entrySet()" separator=",">
                    ${value}
                </foreach>
            </when>
            <otherwise>
                id desc
            </otherwise>
        </choose>
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into llm_skill (name , description , content , status , dept_id , delete_flag , created_by , created_dt , updated_by , updated_dt)
        values (#{name} , #{description} , #{content} , #{status} , #{deptId} , 1, #{createdBy}, #{createdDt}, #{updatedBy}, #{updatedDt})
    </insert>

    <update id="deleteById">
        update llm_skill set delete_flag = -1 , updated_by = #{updatedBy} , updated_dt = #{updatedDt}
        where id = #{id}
    </update>

    <update id="updateById">
        update llm_skill set updated_by = #{updatedBy} , updated_dt = #{updatedDt}
        <if test="name != null and name != ''">
            , name = #{name}
        </if>
        <if test="description != null and description != ''">
            , description = #{description}
        </if>
        <if test="content != null and content != ''">
            , content = #{content}
        </if>
        <if test="status != null">
            , status = #{status}
        </if>
        where id = #{id}
    </update>

    <select id="querySkillCount" resultType="java.lang.Integer">
        select count(1)
        from llm_skill
        where 1 = 1
        <include refid="searchCondition"/>
        ${dataScopeSQL}
    </select>

    <select id="querySkillList" resultType="com.spark.bean.llm.result.SkillResult">
        select <include refid="listColumnList"/>
        from llm_skill
        where 1 = 1
        <include refid="searchCondition"/>
        ${dataScopeSQL}
        order by
        <include refid="sortCondition"/>
        <if test="page == true">
            limit #{startRow}, #{pageSize}
        </if>
    </select>

    <select id="querySkill" resultType="com.spark.bean.llm.result.SkillResult">
        select <include refid="allColumnList"/>
        from llm_skill
        where 1=1
        <include refid="searchCondition"/>
        limit 1
    </select>

    <select id="querySkillByName" resultType="com.spark.bean.llm.result.SkillResult">
        select id , name from llm_skill
        where delete_flag = 1 and name = #{name}
        limit 1
    </select>

    <select id="queryEnabledSkillList" resultType="com.spark.bean.llm.result.SkillResult">
        select id , name , description from llm_skill
        where delete_flag = 1 and status = 1
        and id in (
        <foreach collection="skillIds" item="sid" separator=",">
            #{sid}
        </foreach>
        )
    </select>

    <select id="queryEnabledSkillByName" resultType="com.spark.bean.llm.result.SkillResult">
        select id , name , content from llm_skill
        where delete_flag = 1 and status = 1 and name = #{name}
        and id in (
        <foreach collection="skillIds" item="sid" separator=",">
            #{sid}
        </foreach>
        )
        limit 1
    </select>
</mapper>
