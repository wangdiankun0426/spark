<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.spark.dao.kg.KgGraphDao" >
    <sql id="allColumnList" >
        id , name , description , entity_types , relation_types , extract_model_id , chunk_size , overlap , status ,
        dept_id , delete_flag , created_by , created_dt , updated_by , updated_dt
    </sql>

    <sql id="searchCondition">
        <if test="id != null">
            and id = #{id}
        </if>
        <if test="ids != null and ids.size() > 0">
            and id in (
            <foreach collection="ids" item="id" separator=",">
                #{id}
            </foreach>
            )
        </if>
        <if test="deleteFlag != null and id == null and ids == null">
            and delete_flag = #{deleteFlag}
        </if>
        <if test="name != null">
            and name like concat('%', #{name}, '%')
        </if>
        <if test="status != null">
            and status = #{status}
        </if>
    </sql>

    <sql id="sortCondition">
        <choose>
            <when test="sorts != null and sorts.size() > 0">
                <foreach item="value" index="key" collection="sorts.entrySet()" separator=",">
                    ${value}
                </foreach>
            </when>
            <otherwise>
                id desc
            </otherwise>
        </choose>
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into kg_graph (id, name , description , entity_types , relation_types , extract_model_id , chunk_size , overlap , status ,
        dept_id , delete_flag , created_by , created_dt , updated_by , updated_dt)
        values (#{id}, #{name} , #{description} , #{entityTypes} , #{relationTypes} , #{extractModelId} , #{chunkSize} , #{overlap} , #{status} ,
        #{deptId} , 1, #{createdBy}, #{createdDt}, #{updatedBy}, #{updatedDt})
    </insert>

    <update id="deleteById">
        update kg_graph set delete_flag = -1 , updated_by = #{updatedBy} , updated_dt = #{updatedDt}
        where id = #{id}
    </update>

    <update id="updateById">
        update kg_graph set updated_by = #{updatedBy} , updated_dt = #{updatedDt}
        <if test="name != null and name != ''">
            , name = #{name}
        </if>
        <if test="description != null">
            , description = #{description}
        </if>
        <if test="entityTypes != null">
            , entity_types = #{entityTypes}
        </if>
        <if test="relationTypes != null">
            , relation_types = #{relationTypes}
        </if>
        <if test="extractModelId != null">
            , extract_model_id = #{extractModelId}
        </if>
        <if test="chunkSize != null">
            , chunk_size = #{chunkSize}
        </if>
        <if test="overlap != null">
            , overlap = #{overlap}
        </if>
        <if test="status != null">
            , status = #{status}
        </if>
        where id = #{id}
    </update>

    <select id="queryKgGraphCount" resultType="int">
        select count(1) from kg_graph where 1=1
        <include refid="searchCondition"/>
    </select>

    <select id="queryKgGraphList" resultType="com.spark.common.bean.kg.result.KgGraphResult">
        select <include refid="allColumnList"/>
        from kg_graph
        where 1=1
        <include refid="searchCondition"/>
        order by <include refid="sortCondition"/>
        <if test="page == true">limit #{startRow}, #{pageSize}</if>
    </select>

    <select id="queryKgGraph" resultType="com.spark.common.bean.kg.result.KgGraphResult">
        select <include refid="allColumnList"/>
        from kg_graph
        where 1=1
        <include refid="searchCondition"/>
        limit 1
    </select>
</mapper>
