<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.spark.dao.kg.KgCommunityDao" >
    <sql id="allColumnList" >
        id , graph_id , community_index , name , summary , member_count ,
        dept_id , delete_flag , created_by , created_dt , updated_by , updated_dt
    </sql>

    <sql id="searchCondition">
        <if test="id != null">
            and id = #{id}
        </if>
        <if test="ids != null and ids.size() > 0">
            and id in (
            <foreach collection="ids" item="id" separator=",">
                #{id}
            </foreach>
            )
        </if>
        <if test="deleteFlag != null and id == null and ids == null">
            and delete_flag = #{deleteFlag}
        </if>
        <if test="graphId != null">
            and graph_id = #{graphId}
        </if>
    </sql>

    <sql id="sortCondition">
        <choose>
            <when test="sorts != null and sorts.size() > 0">
                <foreach item="value" index="key" collection="sorts.entrySet()" separator=",">
                    ${value}
                </foreach>
            </when>
            <otherwise>
                community_index asc
            </otherwise>
        </choose>
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into kg_community (graph_id, community_index, name, summary, member_count,
        dept_id, delete_flag, created_by, created_dt, updated_by, updated_dt)
        values (#{graphId}, #{communityIndex}, #{name}, #{summary}, #{memberCount},
        #{deptId}, 1, #{createdBy}, #{createdDt}, #{updatedBy}, #{updatedDt})
    </insert>

    <update id="deleteById">
        update kg_community set delete_flag = -1, updated_by = #{updatedBy}, updated_dt = #{updatedDt}
        where id = #{id}
    </update>

    <update id="updateById">
        update kg_community set updated_by = #{updatedBy}, updated_dt = #{updatedDt}
        <if test="name != null and name != ''">
            , name = #{name}
        </if>
        <if test="summary != null">
            , summary = #{summary}
        </if>
        <if test="memberCount != null">
            , member_count = #{memberCount}
        </if>
        where id = #{id}
    </update>

    <select id="queryKgCommunityCount" resultType="int">
        select count(1) from kg_community where 1=1
        <include refid="searchCondition"/>
    </select>

    <select id="queryKgCommunityList" resultType="com.spark.bean.kg.result.KgCommunityResult">
        select <include refid="allColumnList"/>
        from kg_community
        where 1=1
        <include refid="searchCondition"/>
        order by <include refid="sortCondition"/>
        <if test="page == true">limit #{startRow}, #{pageSize}</if>
    </select>

    <select id="queryKgCommunity" resultType="com.spark.bean.kg.result.KgCommunityResult">
        select <include refid="allColumnList"/>
        from kg_community
        where 1=1
        <include refid="searchCondition"/>
        limit 1
    </select>

    <update id="deleteByGraphId">
        update kg_community set delete_flag = -1, updated_by = #{updatedBy}, updated_dt = now()
        where graph_id = #{graphId} and delete_flag = 1
    </update>
</mapper>
