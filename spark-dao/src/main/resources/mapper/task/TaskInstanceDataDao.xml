<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.spark.dao.task.TaskInstanceDataDao" >
    <sql id="allColumnList" >
        id , set_id , task_id , code , value , delete_flag , created_by , created_dt , updated_by , updated_dt
    </sql>

    <sql id="searchCondition">
        <if test="setId != null and setId != ''">
            and set_id = #{setId}
        </if>
        <if test="taskId != null">
            and task_id = #{taskId}
        </if>
        <if test="code != null and code != ''">
            and code = #{code}
        </if>
        <if test="deleteFlag != null and id == null and ids == null">
            and delete_flag = #{deleteFlag}
        </if>
    </sql>

    <insert id="batchInsert">
        insert into task_instance_data ( set_id , task_id , code , value , delete_flag , created_by , created_dt , updated_by , updated_dt)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.setId}, #{item.taskId}, #{item.code}, #{item.value}, 1, 101, now(), 101, now())
        </foreach>
    </insert>

    <update id="deleteById">
        update task_instance_data set delete_flag = -1 , updated_by = #{updatedBy} , updated_dt = #{updatedDt}
        where id = #{id}
    </update>

    <select id="queryTaskInstanceDataList" resultType="com.spark.bean.task.result.TaskInstanceDataResult">
        select <include refid="allColumnList" />
        from task_instance_data
        where 1=1
        <include refid="searchCondition" />
        order by id
    </select>

</mapper>
