<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.spark.dao.task.TaskInstanceParamDao" >
    <sql id="allColumnList" >
        id , task_id , code , value , delete_flag , created_by , created_dt , updated_by , updated_dt
    </sql>

    <sql id="searchCondition">
        <if test="taskId != null">
            and task_id = #{taskId}
        </if>
        <if test="taskIds != null and taskIds.size() > 0">
            and task_id in
            <foreach item="taskId" index="index" collection="taskIds" open="(" separator="," close=")">
                #{taskId}
            </foreach>
        </if>
        <if test="code != null and code != ''">
            and code = #{code}
        </if>
        <if test="deleteFlag != null and id == null and ids == null">
            and delete_flag = #{deleteFlag}
        </if>
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into task_instance_param (task_id , code , value , delete_flag , created_by , created_dt , updated_by , updated_dt)
        values ( #{taskId} , #{code} , #{value} , 1, #{createdBy}, #{createdDt}, #{updatedBy}, #{updatedDt})
    </insert>

    <update id="updateById">
        update task_instance_param set updated_dt = #{updatedDt}, updated_by = #{updatedBy}
        <if test="value != null">
            ,value = #{value}
        </if>
        where id = #{id}
    </update>

    <update id="deleteById">
        update task_instance_param set delete_flag = -1 , updated_by = #{updatedBy} , updated_dt = #{updatedDt}
        where id = #{id}
    </update>

    <select id="queryTaskInstanceParamList" resultType="com.spark.bean.task.result.TaskInstanceParamResult">
        select <include refid="allColumnList" />
        from task_instance_param
        where 1=1
        <include refid="searchCondition" />
    </select>

</mapper>
